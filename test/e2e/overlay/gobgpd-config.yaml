apiVersion: v1
kind: ConfigMap
metadata:
  name: gobgpd-config
  namespace: bgp-system
data:
  gobgp.conf: |
    [global.config]
      as = 65000
      router-id = "ROUTER_ID_PLACEHOLDER"
      local-address-list = ["NODE_IP_PLACEHOLDER"]
      port = 1790
